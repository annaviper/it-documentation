```
sudo apt install default-mysql-client
```
Manually connect to the MySQL server:
```
mysql -h [IP] -u [username] -p"
```
To quit:
```
exit
```
Options:
- MetaSploit
- nmap's `mysql-enum`

# MetaSploit
